#include "main.h"
/**
 * _abs - a function that compute the absolute value of and integer.
 * @n: The integer to be computed
 *
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
