#include "main.h"
/**
 * _abs - a function that compute the absolute value if an integer
 * @c: integer input
 * Return: Absolute value of ab
 */
int _abs(int ab)
{
	return (ab * ((ab > 0) - (ab < 0)));
}
