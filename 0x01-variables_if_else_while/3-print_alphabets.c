#include <stdio.h>

/**
 * main - prints alphaBETS
 *
 * Return: Always 0 (Success)
 */
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	for (ch 'A'; ch <= 'A'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
